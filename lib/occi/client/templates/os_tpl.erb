<% require 'hashie/mash' %>
<%# We always get an array of OCCI::Collections %>
Available OS templates:
<% occi_resources.each do |occi_os_tpl| %>
  <% os_tpl = occi_os_tpl.as_json %>
  OS template "os_tpl#<%= os_tpl.title %>":
    TITLE:        <%= os_tpl.title %>
    TERM:         <%= os_tpl.term %>
    LOCATION:     <%= os_tpl.location %>
<% end %>
